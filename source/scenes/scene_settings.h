/**
 * Settings scene — build/runtime configuration on the top screen
 * (mock mode, API base URL) with Back / Clear cache / Test network
 * buttons on the bottom screen. Reached via push, not the nav bar.
 * Selected via scene_settings_get().
 */
#ifndef SCENES_SCENE_SETTINGS_H
#define SCENES_SCENE_SETTINGS_H

#include "../core/scene.h"

DECLARE_SCENE(settings);

#endif /* SCENES_SCENE_SETTINGS_H */
