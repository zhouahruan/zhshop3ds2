/**
 * Onboarding/guide scene shown on first launch. Walks the user through
 * the app's controls (touch + gamepad) across several pages.
 *
 * After the guide completes, the user is taken to the home scene.
 * The guide sets a persistent flag so it only shows once.
 */
#ifndef SCENES_SCENE_GUIDE_H
#define SCENES_SCENE_GUIDE_H

#include "../core/scene.h"

DECLARE_SCENE(guide);

#endif /* SCENES_SCENE_GUIDE_H */
