/**
 * Downloads scene — top screen lists download tasks as progress bars (or
 * the installed app list when the Installed tab is active); the bottom
 * screen provides a task/installed toggle and the nav bar. Selected via
 * scene_downloads_get().
 */
#ifndef SCENES_SCENE_DOWNLOADS_H
#define SCENES_SCENE_DOWNLOADS_H

#include "../core/scene.h"

DECLARE_SCENE(downloads);

#endif /* SCENES_SCENE_DOWNLOADS_H */
