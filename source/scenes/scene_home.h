/**
 * Home scene — featured carousel banner plus a grid of recommended apps
 * on the top screen, category tabs and the shared nav bar on the bottom
 * screen. Tapping a recommended app pushes the detail scene with that
 * app's id. Selected via scene_home_get().
 */
#ifndef SCENES_SCENE_HOME_H
#define SCENES_SCENE_HOME_H

#include "../core/scene.h"

DECLARE_SCENE(home);

#endif /* SCENES_SCENE_HOME_H */
