/**
 * Splash scene: shows a 3DS logo + a one-line loading label for ~1.5 s,
 * then auto-pushes the home scene. Selected via scene_splash_get().
 */
#ifndef SCENES_SCENE_SPLASH_H
#define SCENES_SCENE_SPLASH_H

#include "../core/scene.h"

DECLARE_SCENE(splash);

#endif /* SCENES_SCENE_SPLASH_H */
